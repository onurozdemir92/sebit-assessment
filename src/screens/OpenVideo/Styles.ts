import { StyleSheet } from 'react-native';

const Styles = StyleSheet.create({
  container: { height: '100%' },
  videoView: {
    flex: 1,
    backgroundColor: 'black',
  },
  tagsWebView: { flex: 0, height: 100 },
  scrollView: {
    flex: 1,
    marginTop:0
  }
});

export default Styles;
